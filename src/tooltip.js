import React from "react";
import styled from "styled-components";
import PropTypes from "prop-types"
import isFunction 

const Tooltip = props => {



}

Tooltip.propTypes = {


}

Tooltip.defaultProps = {
    icon: null,
    options: "No options provided.  This is probably a mistake."
}

export default Tooltip;
